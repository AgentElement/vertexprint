{
  "dependencies": {
    "@tailwindcss/cli": "^4.3.1",
    "esbuild": "^0.28.1",
    "ml-matrix": "^6.13.0",
    "tailwindcss": "^4.3.1",
    "three": "^0.184.0",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "build:openscad-wasm": "make -C openscad-wasm",
    "build": "npm run build:openscad-wasm && esbuild app.ts --bundle --outfile=a.js --format=esm --target=es2022 --minify --tree-shaking=true --loader:.stl=file && tailwindcss -i style.css -o a.css --minify"
  },
  "devDependencies": {
    "@iconify/json": "^2.2.490",
    "@iconify/tailwind4": "^1.2.3",
    "@types/three": "^0.185.0"
  }
}
